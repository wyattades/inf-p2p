extends ../node_modules/webpack-boiler/template.pug

block page_content
  canvas#game
  #info
  #menu.hidden
    p Paused
    #options
    p
      em Use WASD and the mouse to control the character.
    form#p2p
      textarea
      button(type="submit") Connect
    button#resume(type="button") Resume
    button#clearMapCache(type="button") Clear map cache
  #text-overlay
    p.loader Initializing world
    p.error.hidden !!! ERROR !!!
