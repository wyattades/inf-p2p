import * as THREE from 'three';

export const ZERO_VECTOR2 = Object.freeze(new THREE.Vector2(0, 0));
export const ZERO_VECTOR3 = Object.freeze(new THREE.Vector3(0, 0, 0));
export const ZERO_QUATERNION = Object.freeze(new THREE.Quaternion(0, 0, 0, 1));
